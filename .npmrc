# Default registry for publishing to npm
registry=https://registry.npmjs.org

# GitHub Packages configuration (configured dynamically in workflows)
# @aprestmo:registry=https://npm.pkg.github.com

# Security and dependency management
audit-level=moderate
engine-strict=false
legacy-peer-deps=false
fund=false
save-exact=true

